THIS IS A LUGHATDAD INTERPRETER 

HOW TO RUN: 

Install Flask (pip install flask).

Compile the JavaCC file (javacc LughatDad.jj then javac LughatDad/*.java). ( IT COMES ALREADY COMPILED) 

Run the Flask app from terminal (python app.py).